name: Unit Tests

on: [push, pull_request]

jobs:
  build:
    strategy:
      os: [ubuntu-20.04, macos-10.15]

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@master
        with:
          fetch-depth: 1
          submodules: true
      - name: Run Makefile
        run: make test
